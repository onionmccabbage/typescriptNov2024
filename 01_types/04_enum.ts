enum BoxSize { Small, Medium} // these are 0, 1 in the enum

enum BoxSize { Large=2, XLarge} // carry on the enumeration

let c = BoxSize.Medium

c++ // (not the language) increment c by 1

console.log(c, BoxSize.Small)